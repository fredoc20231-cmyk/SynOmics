ACCEPTANCE_INVALID

See `LIVE_PRODUCT_ACCEPTANCE.md` and `/opt/cursor/artifacts/p12-live/ACCEPTANCE_INVALID.md`.

The reachable `127.0.0.1:3020` process is heuristic with an empty `git_sha` and is not this candidate. Docker Engine is unavailable. Live Chat credentials were not provided.
